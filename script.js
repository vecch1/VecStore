document.getElementById('filter').addEventListener('change', function() {
    const filtro = this.value;
    const produtos = document.querySelectorAll('.main-produtos');
    const relogio = document.querySelectorAll('.produtos-relogio');
    const corrente = document.querySelectorAll('.produtos-corrente');
    const camiseta = document.querySelectorAll('.produtos-camiseta');
    const bone = document.querySelectorAll('.produtos-bone');
    const tenis = document.querySelectorAll('.produtos-tenis');

});